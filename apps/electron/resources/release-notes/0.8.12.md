# v0.8.12 - Qwen Backend Migration

- The app now uses the Qwen backend as its single supported agent runtime.
- Removed legacy multi-provider setup, packaging, onboarding, and runtime paths.
- Updated bundled docs, examples, and release notes so the product surface presents Qwen-only behavior.
- Cleaned dependency metadata and build scripts to avoid shipping unused provider SDKs.
